# Byte-compiled / cache
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.numba_cache/

# Virtual environments
.venv/
.venv-gpu/
venv/
env/
ENV/

# Local HPC environment bootstrap (module loads + venv activation; site-specific)
gpu_env.sh

# Packaging / build artifacts
*.egg-info/
.eggs/
build/
dist/

# Test / coverage output
.coverage
.coverage.*
htmlcov/
.tox/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Simulation output (plots, generated by examples/tests)
*.png

# Stray local tooling (not part of the project)
/gh

# Scaling-study job logs: per-run JSON is the result and is committed, the
# Slurm stdout beside it is not.
profiling/data/helios_scaling/logs/
# GPU benchmark: JSON results are committed, the run logs beside them are not.
profiling/data/gpu_athena/*.log
.venv-gh200/
